apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-app-ingress
spec:
  rules:
  - host: staging.nabil.homes
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: main-app
            port:
              number: 80
